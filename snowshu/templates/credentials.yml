version: '1'
sources:
- name: default
  adapter: snowflake
  account: kic1992.us-east-1
  user: frosty
  password: "such_secure_password"
  # Alternatively, authenticate with key-pair auth instead of a password.
  # Provide the base64-encoded, unencrypted private key body (no
  # "-----BEGIN/END PRIVATE KEY-----" markers, no passphrase support).
  # Exactly one of `password` or `private_key` must be set.
  # private_key: "MIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC7..."
  database: "snowshu"
